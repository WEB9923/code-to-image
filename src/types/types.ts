import {
	backgrounds,
	languages,
	themes
} from "@/constants/constants";

export type TLanguages = typeof languages;
export type TThemes = typeof themes;
export type TBackgrounds = typeof backgrounds;

